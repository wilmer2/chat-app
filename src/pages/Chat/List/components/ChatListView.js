import React from 'react';
import ChatList from './ChatList';

const ChatListView = () => (
  <div className="container">
    <h1 className="text-center mt-5">Chats</h1>
    <ChatList />
  </div>
);

export default ChatListView;
